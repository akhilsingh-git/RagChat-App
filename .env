# Environment variables for local development and migration scripts

# This points to the PostgreSQL database running in Docker.
# Note that we use 'localhost' because the script runs on your host machine,
# not inside the docker network like the app service.
DATABASE_URL=postgresql://raguser:ragpassword@localhost:5432/ragdb